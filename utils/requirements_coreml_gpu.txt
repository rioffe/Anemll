# Requirements for CoreML GPU Conversion Script
# Install with: pip install -r utils/requirements_coreml_gpu.txt

# Core dependencies
coremltools>=8.2
torch>=2.5.0
transformers>=4.39.0
loguru>=0.7.0

# System utilities
psutil>=5.9.0

# Optional but recommended
accelerate>=0.20.0  # For faster model loading
sentencepiece       # For some tokenizers
